{
  "name": "voyager",
  "version": "1.0.0",
  "description": "Voyager is a robust web server that any frontend(web, mobile, or desktop) can plug into.",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "SET NODE_ENV=dev && nodemon index.js",
    "test": "SET \"NODE_ENV=test\" && nodemon --exec \"mocha --recursive -R min\""
  },
  "author": "Asmovic",
  "license": "MIT",
  "dependencies": {
    "body-parser": "^1.19.0",
    "dotenv": "^10.0.0",
    "express": "^4.17.1",
    "mocha": "^9.0.2",
    "mongoose": "^5.13.3",
    "supertest": "^6.1.3"
  },
  "devDependencies": {
    "nodemon": "^2.0.12"
  }
}